%table.table.table-striped
  %thead
    %tr
      %th= model_class.human_attribute_name(:title)
      %th= model_class.human_attribute_name(:text)
      %th= model_class.human_attribute_name(:created_at)
      %th= model_class.human_attribute_name(:creator)
      %th=t '.actions', :default => t("helpers.actions")
  %tbody
    - @announcements.each do |announcement|
      %tr
        %td= link_to announcement.title, announcement_path(announcement)
        %td= announcement.text
        %td= announcement.created_at
        %td= link_to announcement.member.email, member_path(announcement.member)
        %td= action_buttons(announcement)

= link_to t('.new', :default => t("helpers.links.new")), new_announcement_path, class: 'btn btn-primary'