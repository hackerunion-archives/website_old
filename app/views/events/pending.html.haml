%h1 Listing Pending events

%table
  %tr
    %th Title
    %th Description
    %th Start time
    %th End time
    %th Submitted By
    %th

  - @events.each do |event|
    %tr
      %td= event.title
      %td= event.description
      %td= event.start_time
      %td= event.end_time
      %td= link_to event.member.email, member_path(event.member)
      %td= button_to "Approve", approve_event_path(event), method: "put"